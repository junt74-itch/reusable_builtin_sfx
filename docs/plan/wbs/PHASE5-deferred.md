# 対象外（Phase 5）

親計画の Phase 5。Composer 2.5 の MVP 順には入れない。T430 完了後に別 WBS を切る。

- WAV export
- `bun run sfx render|mutate|random`
- preset authoring GUI

本リポジトリを offline asset generator にする話は、Public API と sampleRate 非依存が固まってから。
